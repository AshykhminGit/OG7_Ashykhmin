
#определение функции 1
# даем пользователю ввести две переменные Обозначаем их как целые

#  обозначаем функцию в которой будем волзвращать результат умножения Х
def mult(x): # x  аргумент
    return x # функция будет возвращать результат Х в скобках
a = int (input("введите целое число:"))#  вводим первое число
b = int (input("введите второе целое число:"))#  вводим второе число
#определяем что Х это результат умножения (тут может быть любая арифм операция)
x= a*b


# функция вторая, узбекская
def plus (y):
    return y
y = str(input(" Текст добавляй насяльника!!!"))
y = y+' '+str(mult(x))
print ("Результатамана:",  plus(y))


